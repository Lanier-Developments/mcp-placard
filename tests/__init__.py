"""Test package for Placard.

Declared as a package so ``python -m tests.mock_server`` works from the repo root,
which is how integration tests and the self-gating CI job launch the fixture server.
"""
