{
  "name": "update_record",
  "description": "Update an existing record, guarded by a nested concurrency option.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "id": { "type": "string" },
      "body": { "type": "string" },
      "options": {
        "type": "object",
        "properties": {
          "if_match": { "type": "string" }
        }
      }
    },
    "required": ["id", "body"]
  },
  "expected_tier": "R3",
  "expected_reversibility": "verified"
}
