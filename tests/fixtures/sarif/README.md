# SARIF 2.1.0 schema

`sarif-schema-2.1.0.json` is the OASIS SARIF 2.1.0 (errata 01) JSON schema, vendored
verbatim from https://github.com/oasis-tcs/sarif-spec (`sarif-2.1/schema/`), so
`tests/test_report_sarif.py` can validate Placard's output offline. OASIS Open license.
